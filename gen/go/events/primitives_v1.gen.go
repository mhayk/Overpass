// Code generated by github.com/atombender/go-jsonschema, DO NOT EDIT.

package events

import "time"

// Bid or value in integer credits. Integers, not floats: allocation compares and
// sums these constantly, and float comparison in a tie-break is a bug waiting for
// a specific input. There is no settlement — nothing charges these.
type Credits int

// Opaque customer identifier. Deliberately a stub: there is no authentication or
// multi-tenancy in this system, and the README says so as a scope cut rather than
// leaving it to be discovered.
type CustomerId string

// Elapsed wall-clock milliseconds, for operational metrics carried on events.
type DurationMillis int

// Duration in seconds. Fractional seconds are meaningful here — slew times and
// dwell times are sub-second-sensitive.
type DurationSeconds float64

// NORAD catalog number, as it appears in the TLE. The join key back to Celestrak.
type NoradId int

type PriorityTier string

const PriorityTierBESTEFFORT PriorityTier = "BEST_EFFORT"
const PriorityTierCIVILPROTECTION PriorityTier = "CIVIL_PROTECTION"
const PriorityTierCOMMERCIAL PriorityTier = "COMMERCIAL"
const PriorityTierGOVERNMENT PriorityTier = "GOVERNMENT"

// A normalised 0..1 ratio.
type Ratio float64

// Stable internal satellite identifier, e.g. SENTINEL-1A. Distinct from norad_id
// because a satellite outlives any particular TLE.
type SatelliteId string

// A half-open interval [start, end). JSON Schema cannot express end > start, so
// that check lives in the validating adapter on both sides of every boundary —
// noted here so its absence is not mistaken for an oversight.
type TimeWindow struct {
	// End corresponds to the JSON schema field "end".
	End Timestamp `json:"end" yaml:"end" mapstructure:"end"`

	// Start corresponds to the JSON schema field "start".
	Start Timestamp `json:"start" yaml:"start" mapstructure:"start"`
}

// RFC 3339 timestamp. Always serialised in UTC with a trailing Z. The system has
// exactly one timezone and it is UTC; local time never enters the domain.
type Timestamp time.Time

type Uuid string
