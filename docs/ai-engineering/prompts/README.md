# Prompts

The actual prompts used, not cleaned-up reconstructions. Roles are described in
[`../01-agent-roles.md`](../01-agent-roles.md).

Kept verbatim because a sanitised prompt is not evidence of anything. The
interesting parts are usually the constraints that were added *after* something
went wrong — those are the ones that encode a lesson.
